---
galaxy_info:
  author: Scott Lackey
  description: Installs and configures zookeeper nodes as a cluster
  company: Struct Consulting
  license: BSD
  min_ansible_version: 1.9
  platforms:
  - name: Ubuntu
    versions:
    - trusty
  - name: Debian
    versions:
    - jessie
  - name: RedHat
    versions:
    - all
  categories:
  - zookeeper
  - clustering
  - ELK
dependencies: []
  # List your role dependencies here, one per line. Only
  # dependencies available via galaxy should be listed here.
  # Be sure to remove the '[]' above if you add dependencies
  # to this list.
